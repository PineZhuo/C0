const int e=2, UPPER=200;
int test1,test2;
void strength()
{
   const int a=1;
   int b=3;
}

int power(int x)
{
   int result, i;
   double b = 3E-8;
   result=12;
   if (x<=0)
      return (0);
   i=1;
   while(i<=x)
   {
      result=result*e;
      print("i is:",i ," now result is:",result);
      i=i+1;
   }
   return (result); /* this is a
    multi-notation */
}

void main()
{
   int number, test;
   print("Input a number:");    // this is a single line notation
   scan(number);
   test=power(number);
   print(test);
   if (test<UPPER)
     print("less than upper_line",UPPER-test);
   else
     print("more than upper_line",test-UPPER);
   print("Now we test the nagtive-parameter:");
   print(power(-1));
}